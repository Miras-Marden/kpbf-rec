{
  "name": "kpbf-rec",
  "private": true,
  "packageManager": "pnpm@9.15.1",
  "engines": {
    "node": "20.x",
    "pnpm": ">=9.15.1 <10"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:web": "turbo run build --filter=@kpbf-rec/web",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.3.3",
    "turbo": "^2.0.12"
  }
}

